# ARCHCODE Python dependencies (canonical)
# See pyproject.toml for project metadata and uv.lock for resolved versions.
#
# Python: >=3.11 (tested on 3.11.13)
# Install: pip install -r requirements.txt
# Reproduce: docs/NUMBER_PROVENANCE.md + REPRODUCE.md

# Core numerical stack
numpy>=1.24,<2.0
scipy>=1.11
pandas>=2.0

# Visualization
matplotlib>=3.7

# Statistics & ML
scikit-learn>=1.3

# HTTP (ClinVar API, AlphaGenome API)
requests>=2.31

# Document generation (manuscript .docx export)
python-docx>=1.1

# Testing (used by tests/test_alphagenome_invariants.py)
pytest>=8.0

# Optional — advanced optimization (used by some scripts/*.py)
# optuna>=3.0
