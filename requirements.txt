# ============================================================================
# ARCHCODE v1.0-alpha - Full Stack Dependencies
# ============================================================================

# ----------------------------------------------------------------------------
# 1. Базовый научный стек (обязательный)
# ----------------------------------------------------------------------------
numpy>=1.24.0
scipy>=1.10.0
numba>=0.58.0          # Ускорение симуляции петлевой экструзии ×10–40
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0

# ----------------------------------------------------------------------------
# 2. ML / Deep Learning (для ARCHCODE v1.0-beta)
# ----------------------------------------------------------------------------
torch>=2.0.0            # Grammar Layer: предсказание β(i), λ(i)
einops>=0.7.0           # Удобная работа с последовательностями на 1Mb
transformers>=4.30.0    # Enformer/AlphaGenome-подходы

# ----------------------------------------------------------------------------
# 3. Визуализация
# ----------------------------------------------------------------------------
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# ----------------------------------------------------------------------------
# 4. Специализированные биоинформатические пакеты
# ----------------------------------------------------------------------------
# 4.1 Для работы с ДНК
biopython>=1.81         # Загрузка FASTA-последовательностей реальных локусов
pyfaidx>=0.7.0          # Быстрый доступ к FASTA

# 4.2 Для обработки эпигенетики
pyBigWig>=0.3.18        # Чтение bigWig ChIP-seq и ATAC-seq
cooler>=0.9.0           # Hi-C файловый формат .cool/.mcool
cooltools>=0.5.0        # Анализ TAD, insulation score

# 4.3 Для TE-грамматики
regex>=2023.6.3         # Поиск мотивов CTCF, G4, Z-DNA, WAPL-recruiting
pybedtools>=0.9.0       # Работа с BED файлами транспозонов

# 4.4 Для Non-B DNA логики
# g4predict              # Модель G4Hunter (опционально, требует отдельной установки)
# dna_features_viewer    # Визуализация features (опционально)

# ----------------------------------------------------------------------------
# 5. Инструменты архитектуры / DevOps
# ----------------------------------------------------------------------------
pydantic>=2.0.0         # Строгие модели конфигурации

# ----------------------------------------------------------------------------
# 6. Инструменты для симуляции полимеров (опционально для v1.0)
# ----------------------------------------------------------------------------
# polychrom              # Для 3D расширения (опционально)
# openmm                 # GPU-ускоренная молекулярная динамика (опционально)

# ----------------------------------------------------------------------------
# 7. LTL и формальная верификация
# ----------------------------------------------------------------------------
# temporal-logic         # Расширение LTL ruleset VIZIR (опционально)
# pyeda                  # Логические формулы (опционально)

# ----------------------------------------------------------------------------
# 8. Графики и диаграммы
# ----------------------------------------------------------------------------
graphviz>=0.20.0

# ----------------------------------------------------------------------------
# 9. Разработка и тестирование
# ----------------------------------------------------------------------------
ruff>=0.1.0
mypy>=1.5.0
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-benchmark>=4.0.0
black>=23.0.0
flake8>=6.0.0

# ----------------------------------------------------------------------------
# 10. Дополнительные утилиты
# ----------------------------------------------------------------------------
scikit-learn>=1.3.0     # Для ML-моделей (если потребуется)

