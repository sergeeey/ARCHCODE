# 🔗 Как получить прямые ссылки на файлы

## ⚠️ Важно

Ссылки на порталах могут изменяться или требовать специального формата. Если автоматическая загрузка не работает, используйте этот метод для получения актуальных ссылок.

---

## 📥 Метод 1: Через браузер (рекомендуется)

### Для 4DNucleome файлов (.mcool)

1. **Откройте портал:** https://data.4dnucleome.org/

2. **Найдите файл:**
   - WT: поиск `4DNFI1UEG1O1` или `GM12878 Hi-C`
   - CdLS: поиск `4DNFI9GMP2J8` или `HCT116 auxin RAD21`

3. **Откройте страницу файла**

4. **Найдите кнопку "Download"**

5. **Получите прямую ссылку:**
   - **Вариант A:** ПКМ на "Download" → "Copy link address"
   - **Вариант B:** Откройте DevTools (F12) → Network → Нажмите Download → Найдите запрос → Скопируйте URL

6. **Проверьте формат ссылки:**
   - Должна начинаться с `https://data.4dnucleome.org/`
   - Может содержать `@@download` или `/file/`
   - Пример: `https://data.4dnucleome.org/files-processed/4DNFI1UEG1O1/@@download/4DNFI1UEG1O1.mcool`

### Для GEO файлов (.hic)

1. **Откройте GEO:** https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE95014

2. **Найдите файл:**
   - Перейдите в "Supplementary file"
   - Найдите `GSM2496645` или файл с "HAP1_WAPL_KO"

3. **Скачайте через браузер:**
   - Нажмите на файл
   - Браузер начнёт скачивание
   - В DevTools (F12) → Network найдите запрос к файлу
   - Скопируйте полный URL

4. **Альтернатива - FTP:**
   - Используйте FTP-клиент или браузер
   - URL: `ftp://ftp.ncbi.nlm.nih.gov/geo/samples/GSM2496nnn/GSM2496645/suppl/`
   - Найдите файл `.hic`

---

## 📥 Метод 2: Через curl/wget (для проверки)

Проверьте, работает ли ссылка напрямую:

```powershell
# Проверка через curl
curl -I "https://data.4dnucleome.org/files-processed/4DNFI1UEG1O1/@@download/4DNFI1UEG1O1.mcool"

# Если возвращает HTTP 200 или 302 - ссылка работает
# Если 404 или 403 - нужно получить актуальную ссылку через браузер
```

---

## 📥 Метод 3: Использование API (продвинутый)

### 4DNucleome API

```python
import requests

# Получить информацию о файле через API
accession = "4DNFI1UEG1O1"
api_url = f"https://data.4dnucleome.org/files-processed/{accession}/?format=json"

response = requests.get(api_url)
data = response.json()

# Найдите поле с URL файла
download_url = data.get("download_url") or data.get("file_url")
print(f"Download URL: {download_url}")
```

---

## 🔧 Обновление ссылок в скрипте

После получения актуальных ссылок:

1. **Откройте:** `download_hic_datasets.py`

2. **Найдите секцию DATASETS:**

```python
DATASETS = [
    {
        "name": "WT_GM12878.mcool",
        "url": "https://data.4dnucleome.org/files-processed/4DNFI1UEG1O1/@@download/4DNFI1UEG1O1.mcool",  # ← ВСТАВЬТЕ НОВУЮ ССЫЛКУ
        ...
    },
    ...
]
```

3. **Замените URL** на актуальную ссылку из браузера

4. **Сохраните и запустите:**
```powershell
python download_hic_datasets.py
```

---

## ⚠️ Troubleshooting

### Проблема: Ссылка возвращает 404

**Причины:**
- Файл был перемещён или удалён
- Формат URL изменился
- Требуется авторизация

**Решение:**
1. Получите новую ссылку через браузер (см. Метод 1)
2. Проверьте, что файл существует на портале
3. Попробуйте альтернативные источники (GEO, ENCODE)

### Проблема: Ссылка возвращает 403

**Причины:**
- Требуется авторизация
- IP заблокирован
- Нужны специальные заголовки

**Решение:**
1. Попробуйте скачать через браузер (может потребоваться вход)
2. Используйте VPN
3. Получите прямую ссылку через DevTools

### Проблема: Скачивается HTML вместо файла

**Причина:** Ссылка ведёт на страницу, а не на файл

**Решение:**
1. Найдите прямую ссылку на файл через DevTools
2. Ищите ссылку, которая заканчивается на `.mcool` или `.hic`
3. Проверьте, что ссылка начинается скачивание файла, а не открывает страницу

---

## ✅ Проверка работоспособности ссылки

После получения ссылки проверьте её:

```powershell
# Используйте скрипт проверки
python check_download_urls.py

# Или вручную через curl
curl -I "ВАША_ССЫЛКА"
```

**Ожидаемый результат:**
- HTTP 200: ✅ Ссылка работает
- HTTP 302/301: ⚠️ Редирект (может работать)
- HTTP 404: ❌ Файл не найден
- HTTP 403: ❌ Доступ запрещён

---

## 💡 Альтернативные источники

Если прямые ссылки не работают:

1. **GEO (Gene Expression Omnibus):**
   - https://www.ncbi.nlm.nih.gov/geo/
   - Поиск по GSE номеру
   - Скачивание через веб-интерфейс

2. **ENCODE:**
   - https://www.encodeproject.org/
   - Поиск по названию клеточной линии
   - Прямое скачивание файлов

3. **Ручная загрузка:**
   - Скачайте файлы через браузер
   - Переместите в `data/real/`
   - Переименуйте согласно инструкции




