# Conda environment for ARCHCODE v1.0-alpha
# Create with: conda env create -f environment.yml
# Activate with: conda activate archcode

name: archcode
channels:
  - conda-forge
  - bioconda
  - pytorch
dependencies:
  - python=3.10
  - pip
  - numpy>=1.24.0
  - scipy>=1.10.0
  - numba>=0.58.0
  - pandas>=2.0.0
  - pyyaml>=6.0
  - tqdm>=4.65.0
  - matplotlib>=3.7.0
  - seaborn>=0.12.0
  - plotly>=5.14.0
  - biopython>=1.81
  - pyfaidx>=0.7.0
  - pybigwig>=0.3.18
  - cooler>=0.9.0
  - cooltools>=0.5.0
  - regex>=2023.6.3
  - pybedtools>=0.9.0
  - graphviz>=0.20.0
  - scikit-learn>=1.3.0
  - pip:
    - torch>=2.0.0
    - einops>=0.7.0
    - transformers>=4.30.0
    - pydantic>=2.0.0
    - ruff>=0.1.0
    - mypy>=1.5.0
    - pytest>=7.4.0
    - pytest-cov>=4.1.0
    - pytest-asyncio>=0.21.0
    - pytest-benchmark>=4.0.0
    - black>=23.0.0
    - flake8>=6.0.0







