# 📥 Подробная инструкция по загрузке эталонных Hi-C датасетов

## 🎯 Цель

Загрузить три эталонных датасета для валидации теории RS-11:
1. **WT (GM12878)** — эталон нормы
2. **CdLS-like (HCT116-RAD21-AID)** — аналог NIPBL↓
3. **WAPL-KO (HAP1)** — режим гипер-стабильности

---

## 📋 Подготовка

### Шаг 1: Создайте директорию для данных

```powershell
cd "D:\ДНК КУРСОР"
New-Item -ItemType Directory -Path "data/real" -Force
```

### Шаг 2: Удалите старые ошибочные файлы (если есть)

```powershell
Remove-Item data/real/* -Force -ErrorAction SilentlyContinue
```

### Шаг 3: Убедитесь, что есть свободное место

**Требуется:** минимум **50 GB** свободного места на диске

---

## 📥 ДАТАСЕТ 1: WT (GM12878) - Rao et al., 2014

### Вариант A: Через 4DNucleome Portal (рекомендуется)

1. **Откройте браузер** и перейдите на:
   ```
   https://data.4dnucleome.org/
   ```

2. **В поиске введите:** `4DNFI1UEG1O1` или `GM12878 Hi-C`

3. **Найдите файл** с названием типа:
   - `4DNFI1UEG1O1.mcool`
   - Или файл с пометкой "GM12878" и форматом `.mcool`

4. **Скачайте файл:**
   - Нажмите на файл
   - Выберите "Download" или "Download file"
   - Дождитесь завершения загрузки (может занять 30-60 минут)

5. **Переместите файл:**
   - Переместите скачанный файл в `D:\ДНК КУРСОР\data\real\`
   - Переименуйте в `WT_GM12878.mcool` (если нужно)

### Вариант B: Через GEO (GSE63525)

1. **Откройте:** https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE63525

2. **Найдите раздел "Supplementary file"**

3. **Скачайте Hi-C контактные матрицы:**
   - Ищите файлы с расширением `.hic`, `.cool`, или `.mcool`
   - Может быть несколько файлов — выберите основной для GM12878

4. **Если файл в формате .hic:**
   - Сохраните как `WT_GM12878.hic`
   - Затем конвертируйте (см. раздел "Конвертация")

5. **Если файл уже в формате .cool/.mcool:**
   - Сохраните как `WT_GM12878.mcool`

### Проверка после загрузки:

```powershell
Get-Item data/real/WT_GM12878.mcool | Select-Object Name, @{L='Size(GB)';E={[math]::Round($_.Length/1GB,2)}}
```

**Ожидаемый размер:** 5-10 GB

---

## 📥 ДАТАСЕТ 2: CdLS-like (HCT116-RAD21-AID) - Rao et al., 2017

### Вариант A: Через 4DNucleome Portal

1. **Откройте:** https://data.4dnucleome.org/

2. **В поиске введите:** `4DNFI2TK7L2F` или `HCT116 auxin RAD21`

3. **Найдите файл** с пометкой:
   - "Auxin treated"
   - "RAD21-AID"
   - "Cohesin depletion"
   - Формат: `.mcool`

4. **Скачайте файл** (30-60 минут)

5. **Сохраните как:** `data/real/CdLS_Like_HCT116.mcool`

### Вариант B: Через GEO (GSE104333)

1. **Откройте:** https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE104333

2. **Найдите файлы с пометкой:**
   - "Auxin" или "AID"
   - "RAD21 depletion"
   - **НЕ** используйте файлы "Control" или "WT"

3. **Скачайте файл** для условия с деплецией когезина

4. **Сохраните как:** `data/real/CdLS_Like_HCT116.mcool` (или `.hic`, затем конвертируйте)

### Проверка после загрузки:

```powershell
Get-Item data/real/CdLS_Like_HCT116.mcool | Select-Object Name, @{L='Size(GB)';E={[math]::Round($_.Length/1GB,2)}}
```

**Ожидаемый размер:** 5-10 GB

---

## 📥 ДАТАСЕТ 3: WAPL-KO (HAP1) - Haarhuis et al., 2017

### Через GEO (GSE95014) - основной источник

1. **Откройте:** https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE95014

2. **Найдите раздел "Supplementary file"**

3. **Найдите файл для HAP1 WAPL-KO:**
   - Ищите `GSM2496645` или файлы с пометкой "HAP1_WAPL_KO"
   - Формат: `.hic`

4. **Скачайте файл:**
   - Может называться: `GSM2496645_HAP1_WAPL_KO_inter_30.hic`
   - Или похожее название с "WAPL_KO" и "HAP1"

5. **Сохраните как:** `data/real/WAPL_KO_HAP1.hic`

### Альтернативный источник (FTP):

1. **Попробуйте прямую ссылку:**
   ```
   https://ftp.ncbi.nlm.nih.gov/geo/samples/GSM2496nnn/GSM2496645/suppl/
   ```

2. **Найдите файл** `.hic` для HAP1 WAPL-KO

3. **Скачайте** и сохраните как `data/real/WAPL_KO_HAP1.hic`

### Проверка после загрузки:

```powershell
Get-Item data/real/WAPL_KO_HAP1.hic | Select-Object Name, @{L='Size(GB)';E={[math]::Round($_.Length/1GB,2)}}
```

**Ожидаемый размер:** 3-5 GB

---

## 🔄 Конвертация WAPL файла из .hic в .cool

После загрузки файла `WAPL_KO_HAP1.hic` его нужно конвертировать в формат `.cool`:

### Шаг 1: Установите hic2cool (если еще не установлен)

```powershell
pip install hic2cool
```

### Шаг 2: Конвертируйте файл

```powershell
cd "D:\ДНК КУРСОР"
hic2cool convert data/real/WAPL_KO_HAP1.hic data/real/WAPL_KO_HAP1_10kb.cool -r 10000
```

**Примечание:** Конвертация может занять 10-30 минут в зависимости от размера файла.

### Или используйте готовый скрипт:

```powershell
powershell -ExecutionPolicy Bypass -File convert_hic_to_cool.ps1
```

### Проверка после конвертации:

```powershell
Get-Item data/real/WAPL_KO_HAP1_10kb.cool | Select-Object Name, @{L='Size(GB)';E={[math]::Round($_.Length/1GB,2)}}
```

**Ожидаемый размер:** 1-3 GB

---

## ✅ Финальная проверка всех файлов

После загрузки всех трех датасетов выполните проверку:

```powershell
cd "D:\ДНК КУРСОР"
Write-Host "=== Проверка загруженных файлов ===" -ForegroundColor Cyan
Get-Item data/real/* | Format-Table Name, @{L='Size(GB)';E={[math]::Round($_.Length/1GB,2)}}, @{L='Size(MB)';E={[math]::Round($_.Length/1MB,2)}}, LastWriteTime -AutoSize
```

### Ожидаемый результат:

| Файл | Минимальный размер | Статус |
|------|-------------------|--------|
| WT_GM12878.mcool | > 1 GB | ✅ Готов |
| CdLS_Like_HCT116.mcool | > 1 GB | ✅ Готов |
| WAPL_KO_HAP1.hic | > 100 MB | ✅ Готов (для конвертации) |
| WAPL_KO_HAP1_10kb.cool | > 500 MB | ✅ Готов (после конвертации) |

---

## 🚀 После загрузки: Запуск бенчмарка

Когда все файлы загружены и проверены:

```powershell
cd "D:\ДНК КУРСОР"
python experiments/run_RS11_multi_condition.py
```

Это создаст Figure 4 для всех трех условий!

---

## ⚠️ Troubleshooting

### Проблема: Файлы не скачиваются с портала

**Решения:**
1. Попробуйте другой браузер (Chrome, Firefox, Edge)
2. Отключите VPN (если используется)
3. Попробуйте скачать через FTP (если доступно)
4. Используйте менеджер загрузок (IDM, Free Download Manager)

### Проблема: Файлы в неправильном формате

**Если файл в формате .hic:**
- Используйте `hic2cool` для конвертации (см. раздел "Конвертация")

**Если файл сжат (.gz, .zip):**
```powershell
# Для .gz
gunzip data/real/filename.mcool.gz

# Для .zip
Expand-Archive -Path data/real/filename.zip -DestinationPath data/real/
```

**Если файл в формате .txt или .tsv:**
- Нужна дополнительная конвертация через cooler
- Обратитесь к документации cooler: https://cooler.readthedocs.io/

### Проблема: Недостаточно места на диске

**Решение:**
- Освободите место (минимум 50 GB)
- Можно загружать файлы по одному и удалять после обработки
- Используйте внешний диск для временного хранения

### Проблема: Конвертация hic2cool не работает

**Проверьте:**
1. Установлен ли hic2cool: `pip list | Select-String hic2cool`
2. Правильный ли путь к файлу
3. Достаточно ли места на диске

**Альтернатива:**
- Используйте Juicebox для просмотра .hic файлов
- Или найдите уже сконвертированные файлы на порталах

---

## 📚 Полезные ссылки

- **4DNucleome Portal**: https://data.4dnucleome.org/
- **GEO (Gene Expression Omnibus)**: https://www.ncbi.nlm.nih.gov/geo/
- **Cooler документация**: https://cooler.readthedocs.io/
- **hic2cool GitHub**: https://github.com/4dn-dcic/hic2cool

---

## 📞 Если ничего не помогает

1. Проверьте, что файлы действительно существуют на порталах
2. Попробуйте найти альтернативные источники данных
3. Используйте тестовые данные меньшего размера для проверки пайплайна
4. Обратитесь к авторам оригинальных публикаций за данными

---

## ✅ Чеклист завершения

- [ ] WT_GM12878.mcool загружен (> 1 GB)
- [ ] CdLS_Like_HCT116.mcool загружен (> 1 GB)
- [ ] WAPL_KO_HAP1.hic загружен (> 100 MB)
- [ ] WAPL_KO_HAP1_10kb.cool создан (> 500 MB)
- [ ] Все файлы проверены командой выше
- [ ] Запущен бенчмарк: `python experiments/run_RS11_multi_condition.py`

**Готово! 🎉**


