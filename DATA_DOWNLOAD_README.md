# 📥 Загрузка эталонных Hi-C датасетов

Этот документ описывает процесс загрузки эталонных датасетов для валидации теории RS-11.

## 🎯 Три эталонных датасета

1. **WT (GM12878)** — эталон нормы (Rao et al., 2014)
2. **Cohesin Loss / CdLS-like (HCT116-RAD21-AID)** — аналог NIPBL↓ (Rao et al., 2017)
3. **WAPL-KO (HAP1)** — режим гипер-стабильности (Haarhuis et al., 2017)

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements_rs11.txt
```

### Шаг 2: Загрузка датасетов

**Windows (PowerShell):**
```powershell
powershell -ExecutionPolicy Bypass -File download_datasets.ps1
```

**Linux/Mac/Git Bash:**
```bash
bash download_datasets.sh
```

**Время загрузки:** ~1-2 часа (в зависимости от скорости интернета)  
**Общий объем:** ~20-30 ГБ

Скрипт использует докачку, поэтому можно безопасно прервать и возобновить загрузку.

### Шаг 3: Конвертация WAPL файла

Файл WAPL-KO приходит в формате `.hic` и требует конвертации в `.cool`:

**Windows (PowerShell):**
```powershell
powershell -ExecutionPolicy Bypass -File convert_hic_to_cool.ps1
```

**Linux/Mac/Git Bash:**
```bash
bash convert_hic_to_cool.sh
```

Это создаст файл `data/real/WAPL_KO_HAP1_10kb.cool` с разрешением 10kb.

## 📁 Структура файлов после загрузки

```
data/real/
├── WT_GM12878.mcool              # WT датасет (все разрешения)
├── CdLS_Like_HCT116.mcool         # CdLS-like датасет (все разрешения)
├── WAPL_KO_HAP1.hic               # WAPL-KO исходный файл (.hic)
└── WAPL_KO_HAP1_10kb.cool        # WAPL-KO конвертированный (10kb)
```

## 🔧 Использование в бенчмарках

После загрузки можно запустить multi-condition бенчмарк:

```bash
# WT
python experiments/run_RS11_multi_condition.py \
    --real-cooler "data/real/WT_GM12878.mcool::/resolutions/10000"

# CdLS-like
python experiments/run_RS11_multi_condition.py \
    --real-cooler "data/real/CdLS_Like_HCT116.mcool::/resolutions/10000"

# WAPL-KO
python experiments/run_RS11_multi_condition.py \
    --real-cooler "data/real/WAPL_KO_HAP1_10kb.cool"
```

**Примечание:** Формат `file.mcool::/resolutions/10000` позволяет работать с конкретным разрешением из multi-resolution файла.

## ⚠️ Troubleshooting

### Проблема: `wget` не найден (Windows)

На Windows можно использовать:
- Git Bash (входит в Git for Windows)
- WSL (Windows Subsystem for Linux)
- Или скачать файлы вручную по ссылкам из скрипта

### Проблема: `hic2cool` не установлен

```bash
pip install hic2cool
```

### Проблема: Недостаточно места на диске

Убедитесь, что у вас есть минимум **50 ГБ** свободного места (с запасом).

## 📚 Дополнительная информация

Подробнее о настройке RS-11 см. [RS11_SETUP.md](docs/RS11_SETUP.md)

