# Чеклист для полной валидации ARCHCODE

**Цель:** Выполнить полную валидацию RS-11B, CdLS и создать публикационные фигуры

---

## 📋 Что нужно для полной валидации

### 1. RS-11B: Полная фазовая диаграмма

**Текущее состояние:**
- Прототип: 11×11 точек, 50 циклов
- Время: ~1 минута

**Для полной валидации нужно:**

#### Параметры:
- **Сетка:** 50×50 точек (вместо 11×11)
- **Циклы:** 100+ циклов (вместо 50)
- **Границы:** Больше границ или полный геном (вместо 5)

#### Изменения в коде:
```python
# В experiments/run_RS11B_phase_diagram.py, строка 312:
bookmarking_range=(0.0, 1.0, 50),  # было 11
epigenetic_range=(0.0, 1.0, 50),   # было 11
num_cycles=100,                     # было 50
```

#### Время выполнения:
- **50×50 точек × 100 циклов = 250,000 симуляций**
- **Оценка:** 4-6 часов (зависит от производительности)

#### Команда запуска:
```bash
python experiments/run_RS11B_phase_diagram.py
```

---

### 2. CdLS: Валидация с реальными данными

**Текущее состояние:**
- Использованы WT данные (placeholder)
- Нет реальных CdLS данных

**Для полной валидации нужно:**

#### Данные:
1. **Загрузить реальные CdLS данные:**
   - HCT116 + Auxin (Rao et al., 2017)
   - Формат: `.cool` или `.mcool`
   - Разрешение: 10kb или 100kb

#### Источники данных:
- **4DNucleome:** `4DNFI9GMP2J8` (RAD21-AID HCT116 + Auxin)
- **GEO:** GSE104333 (если доступен)
- **Альтернатива:** Использовать существующие данные из `data/real/`

#### Изменения в коде:
```python
# В experiments/validate_cdls.py:
cdls_cooler_path = "data/real/CdLS_Like_HCT116.mcool::/resolutions/10000"
# Или если есть отдельный файл:
cdls_cooler_path = "data/real_hic/CdLS/CdLS_HCT116_10kb.cool"
```

#### Время выполнения:
- **Анализ реальных данных:** 10-20 минут
- **ARCHCODE симуляция:** 5-10 минут
- **Сравнение и визуализация:** 10-15 минут
- **Итого:** 30-45 минут

#### Команда запуска:
```bash
python experiments/validate_cdls.py --cdls-cooler "путь/к/cdls.cool"
```

---

### 3. WAPL-KO: Дополнительная валидация (опционально)

**Для полной валидации нужно:**

#### Данные:
- WAPL-KO HAP1 данные (уже загружены частично)
- Формат: `.cool` или `.hic`

#### Время выполнения:
- Аналогично CdLS: 30-45 минут

---

## 🚀 План действий для полной валидации

### Шаг 1: Подготовка данных (30 минут)

1. **Проверить наличие CdLS данных:**
   ```bash
   ls data/real/CdLS_Like_HCT116.mcool
   ls data/real_hic/CdLS/
   ```

2. **Если данных нет — загрузить:**
   ```bash
   python download_hic_datasets.py
   # Или вручную с 4DNucleome
   ```

3. **Конвертировать в нужный формат (если нужно):**
   ```bash
   # Если .hic → .cool
   hic2cool convert file.hic file.cool -r 10000
   ```

---

### Шаг 2: Запуск RS-11B с полными параметрами (4-6 часов)

1. **Изменить параметры в скрипте:**
   ```python
   # experiments/run_RS11B_phase_diagram.py, строка 312
   bookmarking_range=(0.0, 1.0, 50),
   epigenetic_range=(0.0, 1.0, 50),
   num_cycles=100,
   ```

2. **Запустить в фоне (рекомендуется):**
   ```bash
   # Windows PowerShell:
   Start-Process python -ArgumentList "experiments/run_RS11B_phase_diagram.py" -WindowStyle Hidden
   
   # Или просто запустить и оставить работать:
   python experiments/run_RS11B_phase_diagram.py > rs11b_log.txt 2>&1
   ```

3. **Мониторить прогресс:**
   ```bash
   # Проверить лог:
   tail -f rs11b_log.txt
   
   # Или проверить размер выходного файла:
   ls -lh data/output/RS11/RS11B_phase_diagram.json
   ```

---

### Шаг 3: Запуск CdLS валидации (30-45 минут)

1. **Найти путь к CdLS данным:**
   ```bash
   # Проверить что есть:
   ls data/real/CdLS_Like_HCT116.mcool
   ```

2. **Запустить валидацию:**
   ```bash
   python experiments/validate_cdls.py \
       --cdls-cooler "data/real/CdLS_Like_HCT116.mcool::/resolutions/10000"
   ```

3. **Если данных нет — использовать WT как placeholder:**
   ```bash
   python experiments/validate_cdls.py \
       --cdls-cooler "data/real_hic/WT/Rao2014_GM12878_1000kb.cool"
   # (с пометкой что это placeholder)
   ```

---

### Шаг 4: Проверка результатов (15 минут)

1. **Проверить RS-11B:**
   - Размер JSON файла должен быть > 1 MB
   - Количество точек в матрице: 50×50 = 2500
   - Критическая линия должна быть найдена

2. **Проверить CdLS:**
   - Визуализация создана
   - Корреляции рассчитаны
   - Сводка сохранена

---

## ⚙️ Технические требования

### Минимальные требования:
- **RAM:** 8 GB (16 GB рекомендуется)
- **CPU:** 4 ядра (8+ рекомендуется)
- **Диск:** 10 GB свободного места
- **Время:** 6-10 часов непрерывной работы

### Оптимальные требования:
- **RAM:** 32 GB
- **CPU:** 8+ ядер
- **SSD:** для быстрого I/O
- **Время:** 4-6 часов

---

## 📊 Ожидаемые результаты

### RS-11B:
- **Файл:** `data/output/RS11/RS11B_phase_diagram.json` (> 1 MB)
- **Фигура:** `data/output/RS11/RS11B_phase_diagram.png`
- **Точки:** 50×50 = 2,500 точек
- **Циклы:** 100 на точку = 250,000 симуляций

### CdLS:
- **Фигура:** `data/output/cdls_validation/cdls_validation.png`
- **Сводка:** `data/output/cdls_validation/cdls_validation_summary.json`
- **Корреляции:** Insulation и P(s)

---

## 🔧 Быстрый старт (скрипт для автоматизации)

Создать скрипт `run_full_validation.py` который:
1. Проверяет наличие данных
2. Запускает RS-11B с полными параметрами
3. Запускает CdLS валидацию
4. Создает итоговый отчет

---

## ✅ Чеклист перед запуском

- [ ] Данные CdLS загружены или доступны
- [ ] Параметры RS-11B изменены (50×50, 100 циклов)
- [ ] Достаточно места на диске (10+ GB)
- [ ] Достаточно RAM (8+ GB)
- [ ] Время для выполнения (6-10 часов)
- [ ] Логирование настроено

---

## 💡 Рекомендации

1. **Запускать на ночь** — RS-11B займет 4-6 часов
2. **Использовать мониторинг** — проверять прогресс каждые 30-60 минут
3. **Сохранять промежуточные результаты** — на случай сбоя
4. **Делать бэкапы** — важные результаты

---

*Готов создать автоматизированный скрипт для полной валидации по запросу.*




