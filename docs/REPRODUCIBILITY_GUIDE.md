# ARCHCODE Reproducibility Guide

**Версия:** 1.0  
**Дата:** 25 ноября 2025  
**Цель:** Воспроизвести весь научный пайплайн ARCHCODE одной командой

---

## 🎯 Быстрый старт

### Требования

- **Python:** 3.9+
- **RAM:** 8 GB минимум (16 GB рекомендуется)
- **Диск:** 10+ GB свободного места
- **Время:** 
  - Fast mode: 15-30 минут
  - Full mode: несколько часов

---

## 📦 Установка

### Вариант 1: Conda/Mamba (рекомендуется)

```bash
# Создать окружение
conda create -n archcode python=3.10 -y
conda activate archcode

# Установить зависимости
pip install -r requirements.txt

# Или если есть environment.yml
conda env create -f environment.yml
conda activate archcode
```

### Вариант 2: Virtualenv

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

---

## 🚀 Запуск пайплайна

### Fast Mode (быстрая проверка)

```bash
python -m archcode.cli run-pipeline --mode fast
```

**Время выполнения:** 15-30 минут  
**Что делает:**
- Unit тесты
- Regression тесты
- RS-09/10/11 симуляции (быстрые параметры)
- Анализ реальных Hi-C данных
- Сравнение ARCHCODE ↔ реальные данные
- Генерация отчёта

### Full Mode (полная валидация)

```bash
python -m archcode.cli run-pipeline --mode full
```

**Время выполнения:** несколько часов  
**Что делает:**
- То же самое, но с полными параметрами
- 50×50 сетки вместо 11×11
- 100 циклов вместо 20
- Полная валидация для публикации

---

## 📊 Ожидаемый результат

После выполнения команды вы получите:

### 1. Результаты экспериментов

```
data/output/pipeline_runs/
├── RS09/
│   └── rs09_results.json
├── RS10/
│   └── rs10_results.json
├── RS11/
│   └── rs11_results.json
├── real_hic_analysis/
│   ├── insulation_scores.csv
│   ├── ps_scaling.csv
│   └── analysis_summary.json
└── comparison/
    ├── archcode_vs_real_comparison.png
    ├── archcode_insulation.csv
    └── comparison_summary.json
```

### 2. Summary Report

```
docs/reports/
└── PIPELINE_SUMMARY_YYYYMMDD_HHMMSS.md
```

Отчёт содержит:
- Статус всех шагов
- Ключевые метрики
- Ссылки на результаты
- Итоговый статус (OK/WARNING/FAIL)

---

## 🔍 Quick Sanity Check

### Проверка установки

```bash
# Проверить Python версию
python --version  # Должно быть 3.9+

# Проверить установленные пакеты
pip list | grep -E "(numpy|pandas|matplotlib|cooler)"

# Проверить структуру проекта
ls -la src/archcode/cli.py
ls -la configs/pipeline_fast.yaml
```

### Минимальный тест

```bash
# Запустить только unit тесты
pytest tests/unit/ -v

# Запустить только regression тесты
pytest tests/regression/ -v

# Запустить одну миссию через адаптер
python tools/run_terag_mission.py --mission terag_missions/rs09_processivity_phase.yaml
```

### Полный пайплайн (fast mode)

```bash
python -m archcode.cli run-pipeline --mode fast
```

**Ожидаемый вывод:**
```
================================================================================
ARCHCODE REPRODUCIBLE SCIENCE PACKAGE v1.0
================================================================================
Mode: fast
================================================================================

================================================================================
STEP 1: UNIT TESTS
================================================================================
✅ Unit tests: 16 passed, 0 failed

================================================================================
STEP 2: REGRESSION TESTS
================================================================================
✅ Regression tests: 3 passed, 0 failed

...

================================================================================
PIPELINE COMPLETE
================================================================================
Total execution time: 1250.45 seconds
Results directory: data/output/pipeline_runs
Summary report: docs/reports/PIPELINE_SUMMARY_20251125_123456.md
================================================================================
```

---

## 📋 Конфигурация

Параметры пайплайна настраиваются через YAML конфиги:

- **Fast mode:** `configs/pipeline_fast.yaml`
- **Full mode:** `configs/pipeline_full.yaml`

### Пример изменения параметров

```yaml
rs09:
  enabled: true
  grid_size: 20  # Увеличить сетку

rs11:
  enabled: true
  cycles: 50  # Увеличить количество циклов
```

---

## 🐛 Troubleshooting

### Проблема: "Config file not found"

**Решение:** Конфиг создастся автоматически при первом запуске. Проверьте `configs/pipeline_fast.yaml`.

### Проблема: "Real Hi-C file not found"

**Решение:** Убедитесь, что файл существует:
```bash
ls data/real_hic/WT/Rao2014_GM12878_1000kb.cool
```

Если файла нет, обновите путь в конфиге или скачайте данные:
```bash
python download_mirror.py
```

### Проблема: "Import errors"

**Решение:** Установите все зависимости:
```bash
pip install -r requirements.txt
```

### Проблема: "Memory error"

**Решение:** Используйте fast mode или уменьшите параметры в конфиге.

---

## 📚 Дополнительная документация

- **API Contract:** `docs/ARCHCODE_TERAG_API.md`
- **TERAG Integration:** `docs/TERAG_INTEGRATION.md`
- **Validation Plan:** `docs/ARCHCODE_FULL_VALIDATION_PLAN.md`
- **Project Report:** `docs/ОТЧЕТ_О_ПРОДЕЛАННОЙ_РАБОТЕ.md`

---

## ✅ Definition of Done

Пайплайн считается воспроизводимым, если:

1. ✅ Команда `python -m archcode.cli run-pipeline --mode fast` выполняется без ошибок
2. ✅ Создаётся summary report в `docs/reports/`
3. ✅ Все результаты сохраняются в `data/output/pipeline_runs/`
4. ✅ На чистой машине можно установить и запустить за 30 минут

---

*Готово к использованию для воспроизведения научных результатов.*


