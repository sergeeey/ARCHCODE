# RS-11: Multichannel Architectural Memory Theory

**Подзаголовок:** Processivity × Bookmarking × Epigenetic Memory

**Статус:** Draft Specification  
**Автор:** ARCHCODE Team  
**Дата:** 23 ноября 2025  
**Зависимости:** RS-09, RS-10, Risk Mitigation Sprint

---

## 📋 СОДЕРЖАНИЕ

1. [Идея RS-11](#1-идея-rs-11-человеческим-языком)
2. [Цели RS-11](#2-цели-rs-11)
3. [Формальные гипотезы](#3-формальные-гипотезы)
4. [Структура экспериментов](#4-структура-rs-11-experiments-ac)
5. [Метрики](#5-метрики)
6. [Структура файлов](#6-структура-файлов--кода)
7. [Выходные данные](#7-выходные-данные)
8. [Готовые фигуры](#8-готовые-фигуры-для-статьи)
9. [Definition of Done](#9-definition-of-done-критерии-завершения-rs-11)
10. [План реализации](#10-план-реализации)

---

## 1. Идея RS-11 (человеческим языком)

Мы уже показали:

* **Processivity** определяет, *насколько* стабильны петли и границы (RS-09).
* **CTCF Bookmarking** определяет, *помнит ли* геном свои границы между митозами (RS-10).

**Теперь задача RS-11:**

Построить модель **"многоканальной памяти архитектуры"**, где:

* **Канал 1** — CTCF-bookmarking (то, что уже есть в RS-10);
* **Канал 2** — эпигенетическая память (гистоны / транскрипционная активность / enhancer memory);
* **Канал 3** (опционально) — "self-organization" через processivity (архитектура сама себя вытягивает без bookmark'ов, если параметры хорошие).

**Цель:**

> Поверх уже существующей оси Processivity × Bookmarking построить полноразмерную модель, которая объясняет, как ядро сохраняет архитектуру сотни клеточных циклов, и где, при каких условиях, эта память ломается.

---

## 2. Цели RS-11

### 2.1. Главная цель

Создать **количественную модель многоканальной архитектурной памяти**, которая:

1. Симулирует **многократные клеточные циклы** (N=50–500+).
2. Учитывает **минимум два независимых канала памяти**:
   * CTCF-bookmarking (структурная память);
   * эпигенетическая/транскрипционная память (функциональная память).
3. Выводит **новый "порог устойчивой памяти"** и **2D/3D-фазовую диаграмму**:
   * Bookmarking_fraction;
   * Epigenetic_memory_strength;
   * Processivity (как контрольный параметр).

### 2.2. Частные цели

* Показать, что **CTCF-only модель** (RS-10) даёт "жёсткий" порог ~30–40% bookmark'ов, но **второй канал памяти** его смягчает.
* Показать режимы:
  * стабильная память (архитектура сохраняется десятки циклов);
  * медленный дрейф;
  * катастрофический распад памяти (memory collapse).
* Связать режимы памяти с **кандидатными биологическими сценариями**:
  * стволовые клетки;
  * дифференцировка;
  * ранний онкогенез;
  * старение (aging).

---

## 3. Формальные гипотезы

### H1: Multichannel Rescue

**Гипотеза:** При фиксированно низком CTCF-bookmarking (ниже чисто CTCF-порога) добавление второго канала памяти (эпигенетика/транскрипция) способно восстановить долгосрочную архитектурную память.

**Предсказание:** В пространстве (Bookmarking_fraction, Epigenetic_strength) существует область, где низкий bookmarking компенсируется высоким epigenetic memory.

---

### H2: Critical Line Instead of Point

**Гипотеза:** В пространстве *(Bookmarking_fraction, Epigenetic_memory_strength)* вместо одного порога существует **критическая линия** (или кривая), разделяющая режимы "память сохраняется" и "память теряется".

**Предсказание:** Фазовая диаграмма показывает плавный переход, а не резкий порог.

---

### H3: Aging Regime

**Гипотеза:** Существуют параметры, при которых архитектура **очень медленно** деградирует (дрейф за десятки–сотни циклов): это теоретическая модель **старения архитектуры ядра**.

**Предсказание:** Найдены траектории с медленным дрейфом (Jaccard падает линейно или экспоненциально медленно).

---

### H4: Processivity as Stabilizer

**Гипотеза:** Высокая processivity может частично компенсировать слабость memory-каналов и задерживать распад архитектуры, но не полностью, если оба канала памяти ниже критической линии.

**Предсказание:** При фиксированных (B, E) увеличение processivity увеличивает время до коллапса, но не предотвращает его полностью.

---

## 4. Структура RS-11: Experiments A–C

### 🧪 RS-11A — Baseline: CTCF + Epigenetic Memory (двухканальная модель)

**Цель:**

Добавить к уже существующему CTCF-bookmarking второй канал памяти — Epigenetic/Transcriptional Memory — и посмотреть, как меняется порог сохранения архитектуры.

#### 4.1. Модель второго канала

**Уровень абстракции:** Простой, но управляемый.

**Вариант 1 (минимальный, рекомендую для начала — RS-11A):**

* У каждого boundary/локуса есть параметр `epigenetic_memory_score` ∈ [0, 1].
* При митозе:
  * CTCF-bookmarked границы восстанавливаются по правилам RS-10 (как сейчас).
  * Для не-bookmarked границ вероятность восстановления зависит от `epigenetic_memory_score`:
    * `P(restore) = f(epigenetic_memory_score)`, линейно или сигмоидно.
* Epigenetic_score может быть статичным (RS-11A) или эволюционирующим (в следующих экспериментах).

**Реализация:**

Новый модуль: `src/archcode_core/epigenetic_memory.py`

Основные функции:

```python
def initialize_epigenetic_memory(boundaries, mode: str, **kwargs):
    """
    Инициализирует epigenetic memory scores для границ.
    
    mode:
      - 'uniform': всем одинаковый level
      - 'random': рандом по заданному распределению
      - 'correlated_with_strength': сильные границы имеют более высокую память
    """
    pass

def restore_with_epigenetic_memory(boundaries, epigenetic_scores, rng, params):
    """
    Восстанавливает часть границ после митоза с учетом epigenetic_scores.
    
    Returns:
        restored_boundaries: список восстановленных границ
        restoration_probabilities: вероятности восстановления для каждой границы
    """
    pass
```

#### 4.2. Экспериментальный дизайн RS-11A

**Параметры:**

* Фиксируем processivity в "разумном" стабильном диапазоне (например, 0.8–1.0).
* Sweep по двум осям:
  * `bookmarking_fraction` ∈ [0.0, 0.1, 0.2, …, 0.6]
  * `epigenetic_strength` ∈ [0.0, 0.25, 0.5, 0.75, 1.0]
    (грубо говоря — насколько сильно этот канал помогает восстановлению границ).

**Для каждой комбинации:**

* Симулируем N циклов: N = 50 (минимум), лучше 100+.
* Метрики:
  * Jaccard (baseline vs cycle N)
  * Drift_distance per cycle
  * Entropy of boundary configuration
  * "Memory_retention_score" (можно как 1 − normalized drift)

**Выходные данные:**

* `data/output/RS11_multichannel_memory_matrix.json`

---

### 🧪 RS-11B — Critical Line & Phase Diagram (полная карта памяти)

**Цель:**

Построить **2D/3D фазовую диаграмму архитектурной памяти**: где память сохраняется, а где умирает.

#### 4.3. Задача

По данным RS-11A:

* Для каждой (bookmarking_fraction, epigenetic_strength) определить, попадает ли система:
  * в режим стабильной памяти (Jaccard > J_thr после N циклов);
  * в режим частичного дрейфа;
  * в режим полного распада (Jaccard → 0).

* Построить:
  * 2D heatmap: память (0–1) по осям (B, E)
  * линию критического перехода (boundary между стабильным и нестабильным режимом)
  * при желании — 3D surface (B, E, Memory_retention).

**Реализация:**

* Скрипт анализа:
  `experiments/analyze_RS11_multichannel_memory.py`
* Скрипт визуализации:
  `experiments/visualize_RS11_phasediagram.py`
* Фигуры:
  * `figures/RS11/RS11_memory_phase_diagram.png`
  * `figures/RS11/RS11_critical_line.png`
  * `figures/RS11/RS11_memory_surface_3d.png`

---

### 🧪 RS-11C — Long-term Aging & Stability (100–1000 циклов)

**Цель:**

Проверить, при каких параметрах архитектура:
* стабильно сохраняется даже за 100–500 циклов,
* медленно деградирует (aging),
* быстро рушится.

#### 4.4. Дизайн

* Выбрать 3–5 характерных точек из фазовой диаграммы:
  * Точка глубоко в зоне стабильной памяти
  * Точка чуть выше критической линии
  * Точка чуть ниже критической линии
  * Точка в зоне полного распада

* Для каждой:
  * Симулировать N_large = 200–500 циклов.
  * Сохранить trajectory:
    * Jaccard(k) vs k
    * entropy(k) vs k
    * drift_distance(k) vs k

* Визуализировать:
  * Кривые aging: `RS11_aging_curves.png`
  * Типы режимов (stable / slow drift / collapse).

**Смысл:**

Получаем **архитектурный аналог старения**: при некоторых параметрах структура ядра "сыпется не сразу, а очень медленно".

---

## 5. Метрики

Для всех экспериментов RS-11 нужны стандартизованные метрики:

### 5.1. Jaccard_stable_boundaries (0–1)

Пересечение стабильных границ baseline vs цикл N.

```python
jaccard = len(intersection(stable_baseline, stable_cycle_N)) / len(union(stable_baseline, stable_cycle_N))
```

### 5.2. Drift_distance

Количество границ, которые сменили категорию (stable → variable → collapsed и наоборот).

```python
drift = count_boundaries_changed_category(baseline, cycle_N)
```

### 5.3. Entropy

Энтропия распределения категорий границ (stable/variable/collapsed).

```python
entropy = -sum(p_i * log(p_i)) for i in [stable, variable, collapsed]
```

### 5.4. Memory_retention_score

Можно определить, например, так:

```python
M = Jaccard * (1 - normalized_entropy)
```

где `normalized_entropy` ∈ [0, 1].

### 5.5. Cycle_to_collapse

Число циклов до того, как Jaccard упадёт ниже порога J_thr (например, 0.3).

```python
cycle_to_collapse = min(k) such that Jaccard(k) < J_thr
```

---

## 6. Структура файлов / кода

### 6.1. Новые модули

**`src/archcode_core/epigenetic_memory.py`**
* `initialize_epigenetic_memory()` — инициализация scores
* `restore_with_epigenetic_memory()` — восстановление границ с учетом epigenetic memory
* `update_epigenetic_memory()` — обновление scores (для будущих экспериментов)

**(Опционально) `src/archcode_core/memory_metrics.py`**
* `calculate_jaccard()` — расчет Jaccard index
* `calculate_entropy()` — расчет энтропии
* `calculate_drift_distance()` — расчет drift
* `calculate_memory_retention_score()` — расчет memory retention score
* `find_cycle_to_collapse()` — поиск цикла коллапса

### 6.2. Эксперименты

**`experiments/run_RS11_multichannel_memory_matrix.py`** (RS-11A)
* Основной скрипт для sweep по (bookmarking_fraction, epigenetic_strength)
* Генерирует матрицу результатов
* Сохраняет в `data/output/RS11_multichannel_memory_matrix.json`

**`experiments/analyze_RS11_multichannel_memory.py`** (RS-11B)
* Анализ матрицы результатов
* Построение фазовой диаграммы
* Нахождение критической линии
* Сохранение в `data/output/RS11_critical_line.json`

**`experiments/run_RS11_aging_regimes.py`** (RS-11C)
* Длительные симуляции для выбранных точек
* Сохранение траекторий
* Сохранение в `data/output/RS11_aging_regimes.json`

### 6.3. Визуализация

**`experiments/visualize_RS11_phasediagram.py`**
* Генерация 2D heatmap фазовой диаграммы
* Построение критической линии
* (Опционально) 3D surface

**`experiments/visualize_RS11_aging.py`**
* Визуализация aging curves
* Разделение режимов (stable / slow drift / collapse)

---

## 7. Выходные данные

### 7.1. Обязательные JSON

**`data/output/RS11_multichannel_memory_matrix.json`**
```json
{
  "parameters": {
    "processivity": 0.9,
    "bookmarking_fractions": [0.0, 0.1, ..., 0.6],
    "epigenetic_strengths": [0.0, 0.25, 0.5, 0.75, 1.0],
    "num_cycles": 100
  },
  "results": {
    "0.0_0.0": {
      "jaccard": 0.15,
      "drift_distance": 8.5,
      "entropy": 0.92,
      "memory_retention_score": 0.12
    },
    ...
  }
}
```

**`data/output/RS11_critical_line.json`**
```json
{
  "critical_points": [
    {"bookmarking": 0.2, "epigenetic": 0.75, "memory_score": 0.5},
    ...
  ],
  "interpolation": "linear" | "spline",
  "threshold": 0.3
}
```

**`data/output/RS11_aging_regimes.json`**
```json
{
  "regime_1": {
    "bookmarking": 0.5,
    "epigenetic": 0.8,
    "trajectory": {
      "cycles": [0, 1, 2, ..., 500],
      "jaccard": [1.0, 0.98, 0.96, ...],
      "entropy": [0.1, 0.12, 0.14, ...],
      "drift_distance": [0, 0.5, 1.2, ...]
    },
    "regime_type": "stable" | "slow_drift" | "collapse"
  },
  ...
}
```

### 7.2. Фигуры

Все фигуры сохраняются в `figures/RS11/`:
* `RS11_memory_phase_diagram.png`
* `RS11_critical_line.png`
* `RS11_aging_curves.png`
* `RS11_memory_surface_3d.png` (опционально)

---

## 8. Готовые фигуры (для статьи)

Минимальный набор для публикации:

### 8.1. RS11_memory_phase_diagram.png

**2D heatmap:** Память (0–1) в пространстве (Bookmarking_fraction, Epigenetic_strength).

**Требования:**
* Publication-quality (300+ DPI)
* Цветовая шкала: синий (низкая память) → красный (высокая память)
* Подписи осей на английском
* Легенда

### 8.2. RS11_critical_line.png

**Линия перехода** между стабильной и нестабильной памятью.

**Требования:**
* Наложение на фазовую диаграмму или отдельный график
* Четкая визуализация границы
* Аннотации режимов (stable / unstable)

### 8.3. RS11_aging_curves.png

**Несколько кривых** Jaccard vs циклы для разных режимов.

**Требования:**
* 3–5 кривых разных цветов
* Легенда с описанием режимов
* Аннотации критических точек

### 8.4. RS11_memory_surface_3d.png (опционально)

**3D поверхность** M(B, E).

**Требования:**
* Четкая визуализация поверхности
* Цветовое кодирование высоты
* Подписи осей

**Эти фигуры можно потом объединить с RS-09/10 в "Two-Axis + Memory" статью.**

---

## 9. Definition of Done (критерии завершения RS-11)

RS-11 считается завершённым, когда:

- [ ] Реализован модуль `epigenetic_memory.py` и интегрирован в cell-cycle pipeline
- [ ] Запущен и успешно выполнен RS-11A (матрица B × E, N циклов ≥ 50)
- [ ] Построена фазовая диаграмма (RS-11B) и найдена критическая линия
- [ ] Выполнены длительные симуляции aging-режимов (RS-11C)
- [ ] Все JSON и фигуры сохранены в `data/output/RS11_*.json` и `figures/RS11/*.png`
- [ ] Написан краткий отчёт: `docs/RS11_SUMMARY.md` с основными выводами
- [ ] Код задокументирован и протестирован
- [ ] Результаты интегрированы в основной отчет проекта

---

## 10. План реализации

### Фаза 1: Инфраструктура (Дни 1-3)

**День 1:**
- [ ] Создать модуль `src/archcode_core/epigenetic_memory.py`
- [ ] Реализовать `initialize_epigenetic_memory()` с тремя режимами
- [ ] Реализовать `restore_with_epigenetic_memory()` с вероятностной логикой
- [ ] Интегрировать в существующий cell-cycle pipeline

**День 2:**
- [ ] Создать модуль `src/archcode_core/memory_metrics.py`
- [ ] Реализовать все метрики (Jaccard, entropy, drift, memory score)
- [ ] Написать unit tests для метрик

**День 3:**
- [ ] Создать базовый скрипт `experiments/run_RS11_multichannel_memory_matrix.py`
- [ ] Реализовать sweep логику
- [ ] Тестирование на малом количестве циклов (N=10)

---

### Фаза 2: RS-11A — Матрица памяти (Дни 4-7)

**День 4:**
- [ ] Запустить полный sweep RS-11A (N=50 циклов)
- [ ] Сохранить результаты в JSON
- [ ] Проверить корректность данных

**День 5:**
- [ ] Анализ результатов RS-11A
- [ ] Проверка гипотезы H1 (multichannel rescue)
- [ ] Подготовка данных для визуализации

**День 6-7:**
- [ ] Создать скрипт визуализации `visualize_RS11_phasediagram.py`
- [ ] Сгенерировать первую версию фазовой диаграммы
- [ ] Итеративная доработка визуализации

---

### Фаза 3: RS-11B — Критическая линия (Дни 8-10)

**День 8:**
- [ ] Создать скрипт анализа `analyze_RS11_multichannel_memory.py`
- [ ] Реализовать алгоритм поиска критической линии
- [ ] Сохранить критические точки в JSON

**День 9:**
- [ ] Визуализация критической линии
- [ ] Проверка гипотезы H2 (critical line instead of point)
- [ ] Доработка визуализации

**День 10:**
- [ ] (Опционально) 3D surface визуализация
- [ ] Финализация фигур для публикации

---

### Фаза 4: RS-11C — Aging режимы (Дни 11-14)

**День 11:**
- [ ] Выбрать характерные точки из фазовой диаграммы
- [ ] Создать скрипт `run_RS11_aging_regimes.py`
- [ ] Запустить длительные симуляции (N=200-500)

**День 12:**
- [ ] Анализ траекторий aging
- [ ] Классификация режимов (stable / slow drift / collapse)
- [ ] Проверка гипотезы H3 (aging regime)

**День 13:**
- [ ] Создать скрипт визуализации `visualize_RS11_aging.py`
- [ ] Сгенерировать aging curves
- [ ] Доработка визуализации

**День 14:**
- [ ] Финализация всех фигур
- [ ] Проверка гипотезы H4 (processivity as stabilizer)
- [ ] Сохранение всех результатов

---

### Фаза 5: Документация и отчет (День 15)

**День 15:**
- [ ] Написать `docs/RS11_SUMMARY.md` с основными выводами
- [ ] Обновить основной отчет проекта
- [ ] Подготовить краткое описание для статьи

---

## 11. Интеграция с существующим кодом

### 11.1. Зависимости от RS-10

RS-11 расширяет функциональность RS-10:

* Использует существующий CTCF bookmarking механизм
* Добавляет второй канал памяти поверх него
* Использует те же метрики (Jaccard, entropy) с расширениями

### 11.2. Интеграция с cell-cycle pipeline

```python
# Псевдокод интеграции
from archcode_core.epigenetic_memory import (
    initialize_epigenetic_memory,
    restore_with_epigenetic_memory
)

# В cell-cycle simulation:
epigenetic_scores = initialize_epigenetic_memory(
    boundaries, 
    mode='correlated_with_strength',
    strength=epigenetic_strength
)

# После митоза:
restored_boundaries = restore_with_epigenetic_memory(
    boundaries,
    epigenetic_scores,
    rng=rng,
    params={
        'bookmarking_fraction': bookmarking_fraction,
        'epigenetic_strength': epigenetic_strength
    }
)
```

---

## 12. Риски и митигация

### Риск 1: Вычислительная сложность

**Проблема:** Длительные симуляции (N=500 циклов) могут быть медленными.

**Митигация:**
* Начать с малого N (N=50)
* Оптимизировать код циклов
* Использовать параллелизацию для sweep

### Риск 2: Параметризация epigenetic memory

**Проблема:** Неясно, как правильно параметризовать epigenetic memory.

**Митигация:**
* Начать с простой модели (линейная зависимость)
* Итеративно усложнять на основе результатов
* Сравнить разные модели

### Риск 3: Интерпретация результатов

**Проблема:** Сложно интерпретировать многоканальную память биологически.

**Митигация:**
* Связать с известными биологическими сценариями
* Сравнить с литературой по эпигенетической памяти
* Обсудить с биологами

---

## 13. Следующие шаги после RS-11

После завершения RS-11 можно:

1. **RS-12:** Интеграция с реальными данными (Hi-C, ChIP-seq)
2. **RS-13:** Динамическая эволюция epigenetic memory
3. **RS-14:** Связь с транскрипционной памятью
4. **RS-15:** Применение к конкретным биологическим сценариям (стволовые клетки, старение)

---

**Последнее обновление:** 23 ноября 2025  
**Статус:** Draft Specification — готов к реализации



