# BASELINE_2026-03-05

Date: 2026-03-05
Scope: Runbook baseline capture before/after P0 safety fixes.

## Initial Baseline (pre-fix)

- Command: `npm run lint`
  - Result: PASS
- Command: `npm run build`
  - Result: PASS
  - Evidence: Build completed with browser externalization warnings from `src/services/AlphaGenomeService.ts`.
- Command: `npm run security:deps`
  - Result: PASS (high/critical gate)
  - Evidence: 4 moderate vulnerabilities (`esbuild/vite/vitest`) remain.
- Command: `npm run security:python`
  - Result: PASS for high
  - Evidence: bandit reported 0 high issues.
- Command: `npm run security:secrets`
  - Result: PASS
- Command: `npm test`
  - Result: FAIL
  - Evidence: 2 failures in `src/__tests__/regression/gold-standard.test.ts`.
- Command: `npm test -- src/__tests__/regression/gold-standard.test.ts`
  - Result: FAIL
  - Evidence:
    - Sox2 Pearson = `0.662223880995218` < 0.7 (`gold-standard.test.ts:215`)
    - Pcdh Pearson = `0.6683391288999453` < 0.7 (`gold-standard.test.ts:296`)
    - Runtime attempted live gRPC and then fallback-to-mock.

## Post-P0 Snapshot (after fix)

- Command: `npm run lint`
  - Result: PASS
- Command: `npm run build`
  - Result: PASS
  - Evidence: Same browser externalization warnings remain (P1 boundary not yet solved).
- Command: `npm run test:gold`
  - Result: PASS
  - Evidence: 12/12 tests passed, explicit test mode logged as `mock`.
- Command: `npm test -- --silent`
  - Result: PASS
  - Evidence: 6 files, 40 tests passed.
- Command: `npm run security:deps`
  - Result: PASS (high/critical gate), 4 moderate remain.
- Command: `npm run security:python`
  - Result: PASS for high.
- Command: `npm run security:secrets`
  - Result: PASS.

## Residual baseline risks

- Moderate advisories in `vite/vitest/esbuild` chain remain.
- Browser/node boundary warnings remain due `AlphaGenomeService` imports.
