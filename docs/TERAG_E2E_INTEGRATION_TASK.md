# 🧩 TERAG × ARCHCODE — End-to-End Integration Task

**Цель:** Довести интеграцию ARCHCODE → TERAG до "боевого" состояния

**Статус:** В работе

---

## 📋 Задачи

1. **TASK 1** — Зафиксировать ARCHCODE ↔ TERAG Data Contract
2. **TASK 2** — Эталонный End-to-End прогон через TERAG-миссию
3. **TASK 3** — Regression-проверка TERAG-миссий

---

## TASK 1 — Зафиксировать ARCHCODE ↔ TERAG Data Contract

### Цель

Сделать один понятный, стабильный формат данных, который ARCHCODE отдаёт, а TERAG читает. Без "магии" и разъезжающихся ключей.

### Что нужно сделать

1. **Создать файл документации:** `docs/ARCHCODE_TERAG_API.md`

2. **Описать JSON-контракты для:**
   - `export_rs09_phase_map()` — оси (processivity/NIPBL/WAPL), значения stability, критические точки
   - `export_rs10_threshold_curve()` — массив точек (bookmarking_fraction, jaccard, estimated_threshold)
   - `export_rs11_memory_surface()` — трёхмерная поверхность (bookmarking × epigenetic × memory)
   - RS12 (sci-Hi-C validation) — метрики (insulation_corr, ps_corr, apa_score)
   - RS13 (multi-condition benchmark) — структура массива условий

3. **Для каждого типа добавить минимальный пример JSON**

4. **Проверить адаптер** (`src/integration/archcode_adapter.py`) — подправить ключи/формат при необходимости

### Критерий готовности (DoD)

- [ ] Файл `docs/ARCHCODE_TERAG_API.md` существует
- [ ] Для RS09, RS10, RS11, RS12, RS13 есть описание полей, типы данных, пример JSON
- [ ] Адаптер возвращает структуры, которые не противоречат документации

---

## TASK 2 — Эталонный End-to-End прогон через TERAG-миссию

### Цель

Показать, что связка TERAG → Adapter → ARCHCODE → Metrics → TERAG реально работает от YAML-миссии до финального ответа.

### Что нужно сделать

1. **Выбрать 1 миссию для эталонного прогона:** RS-11 (`terag_missions/rs11_multichannel_memory.yaml`)

2. **Создать скрипт:** `tools/run_terag_mission.py`
   - Читает путь к YAML-миссии из аргумента
   - Загружает YAML
   - Инициализирует ArchcodeAdapter
   - Вызывает adapter → ARCHCODE
   - Печатает итоговый JSON и статус

3. **Сделать полноценный прогон, сохранить результаты:**
   - JSON: `data/output/RS11_TERAG_E2E_result.json`
   - (опционально) картинка: `figures/RS11/RS11_memory_surface_from_TERAG.png`

4. **Добавить пример:** `docs/examples/RS11_TERAG_E2E.md`
   - Команду запуска
   - Скрин/картинку
   - Пример JSON-фрагмента
   - Краткий комментарий

### Критерий готовности

- [ ] Команда `python tools/run_terag_mission.py --mission terag_missions/rs11_multichannel_memory.yaml` выполняется без ошибок
- [ ] JSON-результат сохранён в `data/output/RS11_TERAG_E2E_result.json`
- [ ] В `docs/examples/RS11_TERAG_E2E.md` задокументирован пример запуска

---

## TASK 3 — Regression-проверка TERAG-миссий

### Цель

Сделать лёгкий regression-тест, который гарантирует, что TERAG-миссии запускаются, адаптер работает, формат данных не сломан.

### Что нужно сделать

1. **Создать regression-скрипт:** `tests/regression/test_terag_missions.py`

2. **Запускать минимум 2 миссии в fast-режиме:**
   - `terag_missions/rs09_processivity_phase.yaml`
   - `terag_missions/rs11_multichannel_memory.yaml`

3. **Проверять:**
   - Статус "success"
   - Наличие ключевых полей в data
   - Отсутствие NaN/None в критичных массивах

4. **Добавить в pytest pipeline**

### Критерий готовности

- [ ] Тесты из `tests/regression/test_terag_missions.py` проходят (PASSED)
- [ ] При изменении формата данных тесты падают → защищают контракт

---

## 🚀 Как использовать это ТЗ

1. Скопировать весь текст ТЗ
2. Вставить в Cursor / другую LLM как задачу
3. Добавить фразу: "Проект уже содержит ARCHCODE, TERAG-adapter и Bio-Metrics Engine. Выполни следующие задачи по этому ТЗ, строго сохраняя структуру каталогов и стилистику кода."

---

*ТЗ готово к использованию. Начинаю выполнение...*


