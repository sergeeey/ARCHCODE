# Velocity Integration Status

**Дата:** 23 ноября 2025  
**Статус:** Интеграция завершена, требуется отладка

---

## ✅ Что реализовано

### 1. Обновлена модель стабильности

**Файлы:**
- `src/boundary_stability/stability_model.py` — добавлен processivity factor
- `src/boundary_stability/factor_aggregator.py` — добавлены поля `nipbl_velocity_factor`, `wapl_lifetime_factor`
- `src/boundary_stability/stability_calculator.py` — добавлено извлечение из VIZIR configs

**Формула обновлена:**
```
Stability = Base × V_nipbl × L_wapl
```

Где:
- `Base` = S_ctcf × (1 + E_barrier) × E_epi × (1 + M_te) × T_time
- `V_nipbl` = NIPBL velocity multiplier (0.0-2.0)
- `L_wapl` = WAPL lifetime multiplier (0.0-2.0)

### 2. Интеграция с VIZIR configs

**Реализовано:**
- Извлечение `nipbl_velocity_multiplier` из P3 config
- Извлечение `wapl_lifetime_factor` из S2 config (если доступно)
- Передача через pipeline

### 3. Эксперимент CdLS

**Файл:** `experiments/run_P3_S2_cdls.py`

**Статус:** Запущен, но разница не видна

---

## 🔍 Текущая проблема

**Симптом:** CdLS simulation показывает одинаковые результаты для WT и CdLS

**Возможные причины:**
1. Значения не передаются через pipeline правильно
2. Значения извлекаются, но не применяются
3. Проблема с копированием конфигов в эксперименте

**Диагностика:**
- Конфиг P3 загружается правильно ✅
- Значения `nipbl_velocity_multiplier` присутствуют ✅
- Значение 0.5 устанавливается в эксперименте ✅
- Но разница в результатах отсутствует ❌

---

## 🔧 Следующие шаги для отладки

### Шаг 1: Проверить передачу через pipeline

Добавить отладочный вывод в:
- `StabilityCalculator._extract_processivity_factors()`
- `StabilityModel.predict()`

### Шаг 2: Убедиться, что значения применяются

Проверить, что:
- `default_nipbl_velocity` правильно устанавливается
- Значения передаются в `aggregate_all_factors()`
- Формула применяется правильно

### Шаг 3: Исправить эксперимент

Возможно, проблема в том, как конфиги копируются и модифицируются в эксперименте.

---

## 📊 Ожидаемые результаты после исправления

**CdLS (velocity = 0.5):**
- Stability должна снизиться на ~50% (или меньше, если есть другие факторы)
- Больше boundaries в категории "variable"
- Увеличение collapse probability

**Пример:**
- WT: avg_stability = 0.6
- CdLS: avg_stability = 0.3 (50% reduction)

---

## ✅ Что работает

1. ✅ Код интегрирован
2. ✅ Формула обновлена
3. ✅ Конфиги загружаются
4. ✅ Эксперимент запускается
5. ⚠️ Результаты не показывают разницу (требует отладки)

---

**Статус:** Готово к отладке  
**Приоритет:** Высокий  
**Следующий шаг:** Отладка передачи значений через pipeline










